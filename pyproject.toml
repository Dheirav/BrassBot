[project]
name = "brassbot"
version = "0.1.0"
description = "A Brass: Birmingham engine and bot"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.setuptools.packages.find]
include = ["brassbot*"]
